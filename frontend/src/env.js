(function(window) {
  window.__env = window.__env || {};

  // API URL - can be overridden by environment-specific config
  window.__env.apiUrl = window.__env.apiUrl || 'http://localhost:3000/api';
}(this));
